---
title: Convert from...
---

# Converting from other formats to `fn`

We'll show you how to convert other formats such as `Make` to `fn`. Writing automation to do this would require a complicated parser, and it may be hard to identify intention, so there will be no migration tools written. A manual conversion may help get more acquainted with `fn` as well, so there's still a silver lining to manual conversion.
